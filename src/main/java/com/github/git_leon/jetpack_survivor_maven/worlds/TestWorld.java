package com.github.git_leon.jetpack_survivor_maven.worlds;

public class TestWorld extends SpriteWorld {
    public TestWorld(int worldWidth, int worldHeight, int cellSize) {
        super(worldWidth, worldHeight, cellSize);
    }
}
